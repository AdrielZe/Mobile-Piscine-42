import HomeScreen from './HomeScreen'; // Importa o HomeScreen do arquivo correspondente
// Registra o componente principal (HomeScreen) no AppRegistry

export default function Index() {
  return <HomeScreen />;
}